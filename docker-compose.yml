version: '3.8'
services:
  dev:
    container_name: server-dev
    image: server-dev:1.0.0
    build:
      context: .
      target: development
      dockerfile: ./Dockerfile
    command: npm run start:debug
    ports:
      - 3000:3000
      - 9229:9229
    networks:
      - server-network
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    restart: unless-stopped
    depends_on:
      - mongodb
    environment:
      #MONGO_URL: mongodb:27017
      DATABASE_URL: mongodb://mongodb:27017/nest_db
      #DATABASE_URL: mongodb://localhost:27047/nest_db

  mongodb:
    image: mongo:latest
    container_name: mongodb
    # environment:
    #   - MONGO_INITDB_ROOT_USERNAME=root
    #   - MONGO_INITDB_ROOT_PASSWORD=pass12345
    networks:
      - server-network
    ports:
      - 27047:27017
    # volumes:
    #   - dbdata6:/data/db
    # healthcheck:
    #   test: echo 'db.runCommand("ping").ok' | mongo localhost:27017/test --quiet
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    restart: unless-stopped

# volumes:
#   dbdata6:

networks:
  server-network:
    driver: bridge
